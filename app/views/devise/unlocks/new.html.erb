<div class="container pt-sm">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      <div class="card shadow zindex-100">
        <div class="card-body px-md-5 py-5">
          <div class="text-center mb-5">
            <h6 class="h3">Resend Unlock</h6>
            <p class="text-muted mb-0">We'll email you another link to unlock your account.</p>
          </div>
          <span class="clearfix"></span>
          <%= render "devise/shared/error_messages", resource: resource %>
          <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
            <div class="form-group">
              <%= f.label :email, class: "form-control-label" %>
              <div class="input-group input-group-transparent">
                <div class="input-group-prepend">
  	  	          <span class="input-group-text">
  	  	          	<i class="fas fa-user"></i>
  	  	          </span>
                </div>
                <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "name@example.com" %>
              </div>
            </div>
            <div class="text-center mb-3">
              <%= f.submit "Unlock Account", class: "btn btn-primary btn-block" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
